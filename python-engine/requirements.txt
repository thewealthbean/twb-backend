# F&O Trader Behavioral Engine — Python dependencies
# Install with: pip install -r requirements.txt

# ── core data processing ──────────────────────────────────────────────────────
pandas>=2.0.0
openpyxl>=3.1.0          # reads .xlsx files
numpy>=1.24.0

# ── web API (optional — only needed to run the REST API server) ───────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9   # for FastAPI file uploads

# ── development / testing ─────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0             # for testing FastAPI endpoints
